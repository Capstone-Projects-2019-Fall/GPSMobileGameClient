using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using SimpleJSON;

public class APITester : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Delta.AddDamage(15);
        // Delta.AddDamage(20);
        // Delta.HealTarget("bob",12);
        // Delta.HealTarget("alice",456);
        // Delta.DrawCardsTarget("carl",1);
        // Delta.DrawCardsTarget("dan",2);
        // Debug.LogFormat("Delta: {0}", Delta.toString());

        // StartCoroutine(APIWrapper.getRoot((response) => {
        //     Debug.LogFormat("Root response: {0}", response);
        // }));
        
        // StartCoroutine(APIWrapper.getSurroundingNodes(60.189010574503,24.9676427387619,(response) => {
        //     Debug.LogFormat("Nodes response: {0}", response);
        // }));

        // StartCoroutine(APIWrapper.getPlayerData("Bob",(response) => {
        //     Debug.LogFormat("Player Data response: {0}", response);
        // }));
        
        //CardFactory.InitializeFactory();
        // StartCoroutine(APIWrapper.syncPlayerDeck("Bob", CardFactory.CreateAllCards(),(response) => {
        //     Debug.LogFormat("Card Sync response: {0}", response);
        // }));

        // StartCoroutine(APIWrapper.createPlayer("Alice", "qwerty",(response) => {
        //     Debug.LogFormat("New Player response: {0}", response);
        // }));

        // StartCoroutine(APIWrapper.getEnemy("Helsinki_South", (response) => {
        //     Debug.LogFormat("Get Enemy response: {0}", response);
        // }));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
